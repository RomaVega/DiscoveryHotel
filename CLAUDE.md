# CLAUDE.md — Orlowsky Discovery Hotel Website

Static marketing site for Orlowsky Discovery Hotel, Candidasa, Bali. **Next.js 15 App Router** with `output: "export"`, deployed on **Netlify** (`orlowsky.id`). All content in JSON — no database, no CMS, no backend.

## Tech Stack & Versions

| Technology | Version | Notes |
|---|---|---|
| Next.js | `^15.5.18` | App Router, SSG (`output: "export"`) |
| React | `^19.0.0` | RSC by default |
| TypeScript | `^5` | Strict mode, no `any` |
| Tailwind CSS | `^4.2.1` | CSS-first config (`@theme` in CSS, not JS config) |
| Framer Motion | `^12.36.0` | Decorative animations only |
| shadcn/ui | CLI `^4.7.0` | `npx shadcn@latest add [component]` |
| @base-ui/react | `^1.3.0` | shadcn primitives — do not import directly |
| Aceternity UI | —  | Copy-paste source only, no npm package |
| lucide-react | `^0.400.0` | Icon library used with shadcn/ui |
| Vitest | `^4.1.0` | Unit tests (jsdom + @testing-library/react) |

> Tailwind CSS 4 uses a CSS-first configuration (`@import "tailwindcss"` + `@theme` block in a `.css` file). There is no `tailwind.config.ts` — design tokens go into `app/globals.css` under `@theme`. See the [v4 migration guide](https://tailwindcss.com/docs/v4-upgrade) if coming from v3.

## Commands

```bash
npx tsc --noEmit              # Type-check
npm run lint                  # Zero warnings required
npm run test                  # Vitest unit tests
npm run build                 # Must pass before deploy (runs prebuild → image manifest)
node scripts/check-images.js  # Catches broken image refs in JSON
npm run check:contrast        # Catches WCAG AA text-contrast regressions
```

Run all six checks before pushing to `main`. There is no bundle-size script yet — the < 500 KB target in [Performance](#performance) is currently aspirational, not enforced.

## Keeping This File Current

**Update this file in the same commit as the change that invalidates it — not "after the push".** Everything below is read as fact by the next person or session, and copied verbatim into new code. A stale line here is worse than no line: it silently propagates the old pattern. This is not hypothetical — the `PrimaryButton` and `SecondaryButton` class strings documented above sat contradicting the code from 10 Aug to 13 Aug 2026, after a contrast fix changed both and left the doc untouched.

Waiting until after a push is already too late: the wrong version is on `main`, deployed, and readable by anyone who starts work in between. Same commit means the doc can never be wrong on `main`, and the diff shows the code change and its documentation together.

A change needs an edit here when it touches:

- **Design tokens or component class strings** — anything quoted in [Design Tokens](#design-tokens-defined-in-appglobalscss-under-theme--never-raw-hex-in-components) or [Key Patterns](#key-patterns). These are quoted literally, so they get copy-pasted while wrong.
- **Commands, scripts, or the check suite** — the list above is the pre-push contract.
- **Folder structure, or a new/renamed `lib/` module** — the data-flow rules depend on it.
- **Env vars, caching, headers, redirects, or deploy config.**
- **Dependency versions** in the Tech Stack table — especially a major bump.
- **Architecture rules** — a new exception to "pages are thin", "no `any`", the Aceternity budget, etc.

Before pushing, skim `git diff origin/main --stat` against that list. If a bullet applies and the doc is untouched in the same diff, fix it before the push rather than after.

Corrections are edits, not appendices — replace the wrong line, don't add a newer one beside it. Two contradicting statements are worse than the original error, because now nobody knows which holds.

## Folder Structure

```
app/                    # Thin page files only — import JSON, compose sections
  ru/                   # Russian-language mirror of every public route (see i18n below)
components/
  ui/                   # shadcn/ui — NEVER edit, wrap instead
  aceternity/           # 2 components only (text-reveal, spotlight-card) — budget is final
  layout/               # Navbar, Footer, WhatsAppButton, InnerPageLayout, LoadingScreen
  sections/             # Page-level sections (HeroImage, RoomsDetail, etc.)
  common/               # Reusable primitives (SectionHeading, FadeIn, PrimaryButton, SecondaryButton)
content/                # All site content as JSON
locales/                # en.json, ru.json — UI strings loaded by LanguageProvider
lib/
  content.ts            # Typed JSON loaders — all pages must use these, never import JSON directly
  types.ts              # Single source of truth for all data shapes
  language-context.tsx  # LanguageProvider + useLanguage hook (t, tl)
  image-loader.ts       # Custom next/image loader — proxies through Netlify Image CDN in prod
  image-manifest.ts     # AUTO-GENERATED by scripts/generate-image-manifest.js (prebuild)
public/images/          # Organized by section
public/video/           # Hero MP4s (desktop + mobile)
scripts/                # check-images.js, check-links.js, check-contrast.js, generate-image-manifest.js
```

### i18n

English routes live at `/foo`, Russian routes at `/ru/foo` — each page is duplicated. Both wrap the tree in `LanguageProvider` (defaultLocale `"en"` or `"ru"`). Content JSON uses `LocalizedString` (`string | { en, ru }`); resolve with `useLanguage().t(...)` for content and `.tl` for UI strings. When editing a page, change **both** the EN and RU copies or accept the drift.

## Architecture Rules

- **Pages are thin.** Import JSON → pass props to sections → return sections in order. No state, no effects, no styling in page files.
- **Data flow:** `content/*.json` → `lib/content.ts` (typed loaders) → page → section → static HTML → CDN.
- **No `any`.** Strict mode. All types in `lib/types.ts`. Add type before adding JSON field.
- **No runtime API calls** (except the GuestPro iframe).
- **Server Components by default.** Only add `"use client"` when component uses hooks/browser APIs. Comment the reason: `"use client"; // Uses IntersectionObserver`.
- **No additional UI libraries.** Only: `framer-motion`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `tw-animate-css`. `@base-ui/react` is a transitive shadcn dep — never import from it directly.
- Aceternity budget is final — no fourth component. Each Aceternity component adds ~15–30 KB and requires copy-paste maintenance with no upstream updates. Build new effects with Framer Motion + CSS.

## Canonical Page Pattern

Inner pages use `InnerPageLayout` (it composes `Navbar`, `Footer`, `WhatsAppButton`). The home pages (`/` and `/ru`) compose those manually because the hero is full-bleed video — see [app/page.tsx](app/page.tsx).

```tsx
// app/rooms/page.tsx — canonical inner-page shape
import { getRoomsPageData, getContactData } from "@/lib/content";
import { InnerPageLayout } from "@/components/layout/InnerPageLayout";
import { PageHero } from "@/components/sections/PageHero";
import { RoomsDetail } from "@/components/sections/RoomsDetail";
import { SITE_URL } from "@/lib/site";

export const metadata = {
  title: "Rooms & Villas in Candidasa, Bali — Orlowsky Discovery Hotel",
  description: "…",
  alternates: { canonical: `${SITE_URL}/rooms` },
  openGraph: { /* … */ },
};

export default function RoomsPage() {
  const data = getRoomsPageData();
  const contact = getContactData();

  return (
    <InnerPageLayout contact={contact}>
      <PageHero
        image="/images/rooms/pool-villa.webp"
        imageAlt="Aerial view of pool villas surrounded by tropical gardens"
        heading={data.heading}
        subtext={data.subtext}
        noOverlay
      />
      <RoomsDetail data={data} />
    </InnerPageLayout>
  );
}
```

Mirror the page under `app/ru/rooms/page.tsx` with translated metadata and `alternates.languages` set for both locales.

## Design Tokens (defined in `app/globals.css` under `@theme` — never raw hex in components)

- `accent-text` — **the teal that may carry text on light grounds.** Resolves to `deep-teal`. Reach for the role, not the hue: the contrast decision is made once in `@theme` rather than at every call site. Enforced by `npm run check:contrast`.
- `brand-teal` (#0abab5) — accent strokes, focus rings, icons, fills. **Never as text on a light ground:** 2.13:1 on sand, 2.27:1 on ivory, 1.88:1 on parchment — it fails AA at every size the site uses, including the 3:1 large-text floor. It *is* correct as text on the dark footer (6.66:1 on espresso), where the polarity flips and `deep-teal` would instead fail at 2.64:1. Mark such cases with a `contrast-ok:` comment carrying the measurement.
- `cta-teal` (#4ca8b5) — softer teal used inside `PrimaryButton` solid fill where the brighter `brand-teal` would clash.
- `deep-teal` (#2a6b74) — hover state, footer, dark sections, scrolled nav.
- `logo-gold` (#c9a84c) — hero star rating and other gilded accents.
- `sand` (#f5f0e8) — page bg | `ivory` (#faf8f4) — card/modal bg | `parchment` (#e8e3d5) — scrolled-nav bg, loading screen.
- `charcoal` (#1a1a1a) — body text, headings | `stone` (#8a8070) — captions, secondary.
- `espresso` (#2a1f17) — footer dark surface.
- `white` (#ffffff) — text on dark bg.

## Typography & Spacing

- **Headings:** Cormorant Garamond (300 display, 400 smaller). Never below 20px.
- **Body/UI:** Inter (400 body, 500 nav/labels, 600 buttons). 16px base, `leading-relaxed`.
- **Spacing:** 8px grid. Sections: `py-24`/`py-32`. Heading mb: `mb-16`. Content: `max-w-7xl` centered. Generous whitespace is the core design principle — never reduce spacing to fit content.

## Animation Rules

- **Scroll fade-in:** opacity 0→1, y 20→0, 0.6s easeOut via `<FadeIn>` wrapper.
- **Stagger:** 0.1s between cards, max 6 steps (0.5s cap).
- **Page transition:** opacity 0.4s, no slides. **Card hover:** scale 1.02, 0.3s. Never ≥1.1.
- **Only animate `opacity` and `transform`.** Never `width`, `height`, `top`, `left`, `margin`.
- Respect `prefers-reduced-motion: reduce` — show static fallback.
- Framer Motion is decorative only. CSS transitions for functional state changes.

## Image Rules

- Always `next/image`, never `<img>`. Full-bleed: `fill` + `sizes="100vw"`. Grid: explicit `width`/`height`.
- Paths in JSON: `/images/...` (not `public/images/...`). Naming: `[section]-[descriptor]-[sequence].ext` — **kebab-case, no spaces** (legacy `Deluxe Cottage/` etc. folders are scheduled to be renamed).
- Every image needs descriptive `alt` from JSON. Decorative: `alt=""` + `aria-hidden="true"`.
- Because `output: "export"` disables Next's built-in optimizer, [lib/image-loader.ts](lib/image-loader.ts) proxies image requests to Netlify Image CDN (`/.netlify/images`) in production. The CDN resizes on demand at the edge and negotiates AVIF/WebP via the Accept header. In dev the loader passes paths through unchanged.
- **Cache-busting is automatic — don't rename a file to force a refresh.** `netlify.toml` serves `images/*` and `video/*` `immutable` for a year under filenames that are not content-hashed, so a file replaced in place would otherwise keep serving the old bytes. The prebuild hashes every referenced image *and* video into `ASSET_VERSIONS` in [lib/image-manifest.ts](lib/image-manifest.ts); the loader appends `?v=<hash>` for images, and `versionedAsset()` from [lib/site.ts](lib/site.ts) for anything that bypasses the loader — chiefly the hero `<video><source>` tags. Any **new** asset type served from `public/` under a long cache (a PDF, a downloadable rate card) needs one of these two paths, or it will go stale for a year. `ASSET_VERSION` in `lib/site.ts` is only the manual fallback for paths assembled at runtime that the build-time scanner cannot see.

## Key Patterns

- **SectionHeading:** optional uppercase label (Inter, brand-teal, `text-sm tracking-widest`), H2 with text-reveal (Cormorant Garamond), optional subtext.
- **PrimaryButton:** `bg-brand-teal hover:bg-cta-teal hover:scale-[1.04] active:scale-[0.97] text-charcoal font-sans font-semibold px-8 py-3 rounded-full tracking-wide uppercase text-sm transition-colors duration-300` + focus-visible ring. See [components/common/PrimaryButton.tsx](components/common/PrimaryButton.tsx) — supports `href` and `external`. **The label is charcoal, not white** (white on `brand-teal` is 2.41:1; charcoal is 7.21:1), and hover is `cta-teal` rather than `deep-teal`, which would drop dark text to 2.86:1. The fill is unchanged — the brand teal stays.
- **SecondaryButton:** `inline-block bg-transparent border border-deep-teal text-accent-text hover:bg-brand-teal hover:text-charcoal transition-all duration-300 px-5 py-2 rounded-full tracking-wide text-xs font-sans font-semibold`. Use for secondary CTAs (e.g., "View All", "Learn More"). Border and text are `deep-teal`/`accent-text`, not `brand-teal`: as text that was 2.13:1 on sand, and the border missed the 3:1 non-text floor too. Do not inline these styles — use the `SecondaryButton` component in `components/common/`. Supports `href` (internal or `external`) and `onClick` (renders as `<button>`). Two call sites cannot use the component (a `<span>` inside a card-wide `<a>`; anchor-in-anchor is invalid HTML) — they import `SECONDARY_BUTTON_BASE` / `SECONDARY_BUTTON_HOVER` from it instead of hand-copying the classes.
- **WhatsApp:** `https://wa.me/[number]?text=[encoded]`. Number from `process.env.NEXT_PUBLIC_WHATSAPP_NUMBER`, international format, no `+`, no spaces. Never hardcode the number in components.
- **GuestPro:** iframe from `https://secure.guestpro.net/odch`. On failure/timeout >5s, fall back to "Book Now" button opening URL in new tab.
- **Gallery:** all images come from `galleryPreview.images` in [content/home.json](content/home.json) — one array feeding both surfaces, so a photo added or removed there changes both at once. [GalleryMosaic](components/sections/GalleryMosaic.tsx) (`/gallery`) renders the full set in a CSS-columns masonry with category filter pills; [GalleryPreview](components/sections/GalleryPreview.tsx) (home) shows one slide plus a thumbnail grid. Both are fully SSG — no lazy slicing, no sentinel, no network. Lightbox via shadcn Dialog with arrow-key nav.

## Accessibility (WCAG 2.1 AA — non-negotiable)

- 4.5:1 body text, 3:1 large text. Focus: `focus-visible:ring-2 focus-visible:ring-brand-teal focus-visible:ring-offset-2`.
- Semantic HTML, one `<h1>` per page. Focus trap in modals, return focus on close.
- Skip link as first focusable element. Hero video: `muted autoPlay playsInline` + pause button.

## Performance

Targets: LCP < 2.5s (`priority` + `<link rel="preload">` on hero poster), CLS < 0.1, INP < 200ms, first-load JS < 500 KB. The bundle target is not yet enforced in CI — eyeball it from `npm run build` output.

## Git & Deploy

- Branches: `feat/`, `fix/`, `content/` + short description. Squash-merge to `main`.
- Commits: imperative, lowercase, ≤72 chars. E.g., `add spa treatment grid section`.
- Push to `main` → Netlify auto-deploys to `orlowsky.id` (30–90s); PRs get deploy-preview URLs.
- Build config: [netlify.toml](netlify.toml) (`npm run build` → publish `out/`). Security headers (HSTS, CSP, X-Frame-Options) and long-cache headers for `_next/static`, `images/*`, `video/*` live there too.

## Content Editing

- All changes are JSON edits in `content/`. **Add type in `lib/types.ts` before adding JSON fields.**
- Offers: set `"active": false` — never delete expired offers.
- New page: create page file (follow canonical pattern) → add to navbar → create sections → add JSON + types → verify with `tsc && build`.

## Environment Variables

- `NEXT_PUBLIC_WHATSAPP_NUMBER` (required) — used in all WhatsApp links, read via `process.env.NEXT_PUBLIC_WHATSAPP_NUMBER`. This is the single source of truth for the number; do not hardcode it elsewhere.
- `NEXT_PUBLIC_GA_MEASUREMENT_ID` (optional) — GA4 property. [components/layout/GoogleAnalytics.tsx](components/layout/GoogleAnalytics.tsx) renders nothing when unset, so dev/previews stay out of the data. Scope it to Netlify's production context. Adding another Google/analytics origin means widening `script-src`/`connect-src` in [netlify.toml](netlify.toml).
- `GOOGLE_SITE_VERIFICATION` (optional) — Search Console HTML-tag token, emitted via `metadata.verification.google`. Not `NEXT_PUBLIC_`; it is build-time only. Safe to drop once DNS TXT verification is in place.
- Never put secrets in `NEXT_PUBLIC_` — they're inlined into client JS.

## Common Mistakes

- Don't put logic (`useState`/`useEffect`/conditionals) in page files — move to section components.
- Don't add `"use client"` to fix type errors — fix the types instead.
- Don't nest `<section>` elements — sections are siblings within `<main>`.
- Don't skip `alt` attributes or use non-descriptive text.
